{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared capability for the main application window.",
  "windows": ["main"],
  "platforms": ["macOS", "windows"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
